_target_: src.data.linear_datamodule.LinearDataModule
num_features: 10
true_weights_seed: 42
noise_sigma: 0.01
batch_size: 128 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [55_000, 5_000, 10_000]
num_workers: 4
pin_memory: False
